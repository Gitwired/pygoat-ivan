name: hello-world
on;
  push;
   branches: [master]
  workflow_dispatch;

  jobs;
    say-hello;
      name: Print Hello world
      runs-on: ubuntu-latest

        steps;
          - name: Say Hello
          run: 
